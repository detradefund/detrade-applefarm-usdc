name: Push NAV for DeTrade AppleFarm USDC

on:
  push:
    branches:
      - main
  # schedule:
  #   - cron: '0 * * * *'  # Runs every hour
  workflow_dispatch:  # Allows manual triggering

jobs:
  run-pusher:
    runs-on: ubuntu-latest
    env:
      ETHEREUM_RPC: ${{ secrets.ETHEREUM_RPC }}
      BASE_RPC: ${{ secrets.BASE_RPC }}
      ETHERLINK_RPC: ${{ secrets.ETHERLINK_RPC }}
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Debug - List directory structure
      run: |
        echo "Current directory structure:"
        ls -la
        echo "\nBuilder directory:"
        ls -la builder/
        
    - name: Debug - Check environment variables
      env:
        ETHEREUM_RPC: ${{ secrets.ETHEREUM_RPC }}
        BASE_RPC: ${{ secrets.BASE_RPC }}
        ETHERLINK_RPC: ${{ secrets.ETHERLINK_RPC }}
      run: |
        echo "Checking environment variables:"
        echo "ETHEREUM_RPC exists: ${{ env.ETHEREUM_RPC != '' }}"
        echo "BASE_RPC exists: ${{ env.BASE_RPC != '' }}"
        echo "ETHERLINK_RPC exists: ${{ env.ETHERLINK_RPC != '' }}"
        echo "MONGO_URI exists: ${{ secrets.MONGO_URI != '' }}"
        echo "DATABASE_NAME_1 exists: ${{ secrets.DATABASE_NAME_1 != '' }}"
        echo "COLLECTION_NAME exists: ${{ secrets.COLLECTION_NAME != '' }}"
        echo ""
        echo "RPC Endpoints (masked):"
        echo "ETHEREUM_RPC length: ${#ETHEREUM_RPC}"
        echo "BASE_RPC length: ${#BASE_RPC}"
        echo "ETHERLINK_RPC length: ${#ETHERLINK_RPC}"
        
    - name: Run pusher
      env:
        ETHEREUM_RPC: ${{ secrets.ETHEREUM_RPC }}
        BASE_RPC: ${{ secrets.BASE_RPC }}
        ETHERLINK_RPC: ${{ secrets.ETHERLINK_RPC }}
        MONGO_URI: ${{ secrets.MONGO_URI }}
        DATABASE_NAME_1: ${{ secrets.DATABASE_NAME_1 }}
        COLLECTION_NAME: ${{ secrets.COLLECTION_NAME }}
        PRODUCTION_ADDRESS: ${{ secrets.PRODUCTION_ADDRESS }}
      run: |
        echo "Running pusher with Python version:"
        python --version
        python -m builder.pusher 